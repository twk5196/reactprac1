public: 가상 DOM을 사용하는 리액트는 실제 DOM이 필요합니다. 
즉, 가상 DOM이 들어갈 빈 껍데기 html이 필요하다는 것인데, 바로
 그 빈 껍데기가 존재하는 폴더입니다.

src: 리액트 개발이 이루어지는 메인 폴더입니다. 

react는 component로 이루어짐 

component는 html을 반환하는 함수

jsx 는 react에서만 유일하게 사용하는 개념

jsx: js안에서의 html  component를 만들고 어떻게 사용하는지에대한것 

react 장점 1component재사용 가능 즉 component 반복해서 사용가능 

props 는 component 의 매개변수 
props를 통해 데이터를 각 컴포넌트마다 넘긴다.

컴포넌트 변수 이름시작은 대문자로 시작


map :여러가지 사용방법이 있지만 보통 
각 배열에 아이템에다가 함수를 적용하고 배열을 반환해준다.
ex: ~~~.map(x => x*2); 
결과 각 배열에 *2한 배열 만들어짐 

